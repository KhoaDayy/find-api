[[["Where Winds Meet - China Version API Hunter\");\n   \n   // China version specific path\n   const GAME_PATH = \"C:\\\\Program Files\\\\yysls\";\n   const GAME_EXE = \"WhereWindsMeet.exe\"; // or actual exe name\n   \n   // Check if game is running\n   var gameProcess = findGameProcess();\n   if (!gameProcess) {\n       console.log(\"[!] Game not found. Please start Where Winds Meet (China version)\");\n       return;\n   }\n   \n   // Initialize with China-specific settings\n   initChinaVersion();\n   ```\n\n### Phase 2: China Version Analysis (1-2 ngu00e0y)\n\n1. **Game Structure Analysis**\n   ```javascript\n   function analyzeGameStructure() {\n       // List files in game directory\n       // Find main executable\n       // Identify protection layers\n   }\n   \n   function findGameProcess() {\n       var processes = Process.enumerateThreads();\n       for (var i = 0; i < processes.length; i++) {\n           if (processes[i].name.includes(\"WhereWinds\") || \n               processes[i].name.includes(\"yysls\")) {\n               return processes[i];\n           }\n       }\n       return null;\n   }\n   ```\n\n2. **Anti-Cheat Detection (China Version)**\n   - **TenProtect (TP)** - Phu1ed5 biu1ebfn trong game Trung\n   - **GameGuard** - Cu00f3 thu1ec3 du00f9ng\n   - **Custom Protection** - Anti-cheat riu00eang\n\n### Phase 3: China-Specific Stealth (2-3 ngu00e0y)\n\n1. **Bypass TenProtect**\n   ```javascript\n   function bypassTenProtect() {\n       // TP often hooks kernel APIs\n       // Need to work at lower level\n       \n       // Hide from TP scans\n       if (Process.findModuleByName(\"TenProtect.dll\")) {\n           console.log(\"[!] TenProtect detected\");\n           // Apply specific bypass\n       }\n   }\n   \n   function chinaSpecificStealth() {\n       // China games often have:\n       // - Memory integrity checks\n       // - Thread creation monitoring\n       // - API hooking detection\n       \n       // Countermeasures:\n       // - Use native functions\n       // - Hide in system threads\n       // - Encrypt all hooks\n   }\n   ```\n\n2. **Server Region Detection**\n   ```javascript\n   function detectServerRegion() {\n       // China servers usually use:\n       // - Different domains\n       // - Different protocols\n       // - Different authentication\n       \n       var chinaPatterns = [\n           /\\.cn\\//,\n           /\\.com\\.cn/,\n           /qq\\.com/,\n           /wegame\\.com/\n       ];\n   }\n   ```\n\n### Phase 4: API Extraction for China Version (3-4 ngu00e0y)\n\n1. **China API Patterns**\n   ```javascript\n   // China version might use:\n   var chinaAPIPatterns = [\n       \"/api/v1/user/info\",      // Common pattern\n       \"/profile/query\",         // Alternative\n       \"/user/getProfile\",       // Another variant\n       \\\"getUserInfo\\", "Direct function\n       \\\"queryUserInfo\\", "Another name\n       \"/cgi-bin/user_info\"      // CGI style\n   ];\n   ```\n\n2. **Authentication Differences**\n   ```javascript\n   // China version might use:\n   var chinaAuthMethods = {\n       qqLogin: true,           // QQ login integration\n       wechatAuth: true,        // WeChat login\n       phoneVerification: true, // Phone verification\n       deviceId: true          // Device binding\n   };\n   ```\n\n### Phase 5: Testing with China Servers (2-3 ngu00e0y)\n\n1. **Test Account Setup**\n   - Use Chinese phone number if possible\n   - QQ account for testing\n   - Avoid main account\n\n2. **Network Differences**\n   ```javascript\n   // China servers characteristics:\n   var chinaServerConfig = {\n       encryption: \"higher\",      // Usually stronger encryption\n       compression: \"yes\",         // Data compression\n       protocol: \"HTTP/2\",        // Might use HTTP/2\n       cdn: \"yes\",               // CDN usage\n       monitoring: \"high\"         // High monitoring\n   };\n   ```\n\n## Complete Script Template\n\n```javascript\n// where_winds_meet_china.js\n(function() {\n    \"use strict\";\n    \n    // China version configuration\n    const CONFIG = {\n        GAME_PATH: \"C:\\\\Program Files\\\\yysls\",\n        TARGET_EXE: \"WhereWindsMeet.exe\",\n        REGION: \"china\",\n        STEALTH_LEVEL: \"maximum\",\n        LOG_TO_FILE: true,\n        ENCRYPT_LOGS: true\n    };\n    \n    // Initialize China version\n    function initChinaVersion() {\n        console.log(\"[*] Initializing Where Winds Meet (China) API Hunter\");\n        \n        // Check for China-specific anti-cheat\n        checkChinaAntiCheat();\n        \n        // Apply China-specific bypass\n        applyChinaBypass();\n        \n        // Setup hooks\n        setupChinaHooks();\n    }\n    \n    // Check for anti-cheat\n    function checkChinaAntiCheat() {\n        var suspiciousModules = [\n            \"TenProtect.dll\",\n            \"TP.dll\",\n            \"GameGuard.dll\",\n            \"HackShield.dll\",\n            \"XIGNCODE3.dll\"\n        ];\n        \n        suspiciousModules.forEach(function(module) {\n            if (Process.findModuleByName(module)) {\n                console.log(\"[!] Detected: \" + module);\n                // Apply specific bypass\n            }\n        });\n    }\n    \n    // Main execution\n    setTimeout(initChinaVersion, Math.random() * 5000 + 3000);\n    \n})();\n```\n\n## API Differences - Global vs China\n\n| Feature | Global Version | China Version |\n|---------|---------------|---------------|\n| Domain | .com | .cn / .com.cn |\n| Auth | Email/Phone | QQ/WeChat/Phone |\n| Encryption | Standard | Enhanced |\n| Monitoring | Normal | High |\n| API Endpoints | /api/v1/ | /cgi-bin/ or /api/ |\n| Response Format | JSON | JSON (maybe different) |\n\n## Launch Command\n\n```bash\n# For China version\nfrida -n \"WhereWindsMeet.exe\" -l where_winds_meet_china.js --no-pause\n\n# Or by PID\nfrida -p <PID> -l where_winds_meet_china.js --no-pause\n```\n\n## Important Notes for China Version\n\n1. **Higher Risk** - China games have stricter anti-cheat\n2. **Different Authentication** - May need QQ/WeChat login\n3. **Stronger Encryption** - Might need to decrypt\n4. **Legal Considerations** - Be aware of local regulations\n\n## Success Metrics\n\n- [ ] Find profile lookup endpoint\n- [ ] Extract request format\n- [ ] Understand auth method\n- [ ] **Account remains safe**\n\nReady to proceed with the China version? Remember to use a test account!", "style\":", "fill:#f9f,stroke:#333,stroke-width:2px\n    style C fill:#ff9,stroke:#333,stroke-width:2px\n    style D fill:#f99,stroke:#333,stroke-width:2px\n    style E fill:#9f9,stroke:#333,stroke-width:2px\n```\n\n## Additional Resources\n\n- China Game Reversing Forums\n- TenProtect Bypass Techniques\n- QQ/WeChat API Documentation\n- Chinese Game Security Papers"]]]